{
  "name": "turnbeck-alerts-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "test": "node --test test/*.test.mjs",
    "schema:local": "wrangler d1 execute turnbeck-alerts --local --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.118.0",
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.4"
  }
}
